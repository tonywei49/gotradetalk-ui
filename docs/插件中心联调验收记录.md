# 插件中心联调验收记录

## 一、联调范围

本次联调范围：

- 平台后台 `Plugin Center` 已提供接口
- 前端插件宿主 `plugin-lab`
- 前端对齐以下接口：
  - `GET /platform/plugins/my-plugins`
  - `GET /platform/plugins/:id/config`
  - `POST /platform/plugins/:id/token`
  - `POST /platform/plugins/:id/usage`

## 二、前端对齐修正

### 1. `my-plugins` 返回结构修正

后端返回结构为：

```json
{
  "items": []
}
```

前端已按 `items` 读取，不再按旧的 `plugins` 字段读取。

### 2. `code` / `plugin_id` 映射修正

当前内建插件使用：

- `code = plugin-lab`

平台接口执行时使用：

- `plugin_id = uuid`

前端已补齐映射策略：

- 插件显示与启用：按 `code`
- 配置读取 / token 签发 / usage 上报：按真实 `plugin_id`

## 三、联调环境

本次验证方式：

- 本地前端开发环境
- 真实平台接口
- 真实 staff 账号

验证账号：

- 公司：`hululucky`
- 用户：`test.jack`

## 四、真实联调结果

### 1. 登录后插件列表读取成功

结果：

- `Plugin Lab` 在设置页显示
- 页面状态显示：
  - `Source: platform`
  - `State: ready`

结论：

- `GET /platform/plugins/my-plugins` 已成功
- 当前公司授权已生效
- 前端已正确吃到平台插件列表

### 2. 插件配置读取成功

点击：

- `Load plugin config`

页面成功显示配置：

```json
{
  "entry": "/plugin-lab",
  "theme": "green",
  "features": ["read", "invoke"]
}
```

同时页面显示：

- `quota_used: 2`

结论：

- `GET /platform/plugins/:id/config` 成功
- `POST /platform/plugins/:id/usage` 成功

### 3. 插件 token 签发成功

点击：

- `Issue plugin token`

页面成功显示：

- token 预览
- `expires_at`

同时页面显示：

- `quota_used: 3`

结论：

- `POST /platform/plugins/:id/token` 成功
- `POST /platform/plugins/:id/usage` 成功

## 五、验收结论

本次联调通过：

- 插件入口显示正常
- 平台插件列表读取正常
- 插件配置读取正常
- 插件专属 token 签发正常
- usage 上报正常
- `quota_used` 可随调用增长
- 前端 `code -> plugin_id` 映射正确

结论：

**前端插件宿主已完成与 2.1 插件中心接口的第一轮联调。**

## 六、当前已知非插件问题

以下问题在本次联调中观察到，但不属于插件接口本身：

### 1. 本地 notebook 接口存在 CORS

本地开发环境下，浏览器直连：

- `https://notebook-api.hululucky.com/...`

会出现 CORS 拦截，导致页面提示：

- `Notebook 能力讀取失敗，請稍後重試。`

说明：

- 这是本地联调环境问题
- 不影响本次插件接口联调结论

### 2. Matrix TURN 接口 404

接口：

- `/_matrix/client/v3/voip/turnServer`

说明：

- 与语音/通话有关
- 不影响插件中心联调

## 七、后续建议

建议下一步：

1. 将本次前端插件联调代码推送并部署
2. 在部署环境再次验证一次 `plugin-lab`
3. 后续再决定是否继续扩展到聊天输入框 slot / 消息操作 slot

